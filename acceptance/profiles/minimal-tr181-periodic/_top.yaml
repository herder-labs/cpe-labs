# Minimal TR-181 acceptance profile with periodic Inform enabled.
#
# Identical to acceptance/profiles/minimal-tr181/ except it declares
# periodicInformPaths and an interval of 1 second (the scheduler's
# enforced minimum) so the test wall-clock budget stays bounded.
# A trimmed periodic ParameterList keeps the golden focused.

deviceIdPaths:
  manufacturer: Device.DeviceInfo.Manufacturer
  oui:          Device.DeviceInfo.ManufacturerOUI
  productClass: Device.DeviceInfo.ProductClass
  serialNumber: Device.DeviceInfo.SerialNumber

periodicInformPaths:
  interval: Device.ManagementServer.PeriodicInformInterval
  enable:   Device.ManagementServer.PeriodicInformEnable

fleet:
  count: 1
  serialPattern: "{base}"

informParameters:
  bootstrap:
    - Device.DeviceInfo.Manufacturer
    - Device.DeviceInfo.ManufacturerOUI
    - Device.DeviceInfo.ProductClass
    - Device.DeviceInfo.SerialNumber
    - Device.DeviceInfo.SoftwareVersion
    - Device.DeviceInfo.HardwareVersion
  periodic:
    - Device.DeviceInfo.SoftwareVersion
    - Device.DeviceInfo.HardwareVersion
